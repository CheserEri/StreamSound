# StreamSound · 私有流媒体音乐播放器
## 产品需求文档 (PRD) v1.0

| 字段 | 内容 |
|------|------|
| 文档状态 | 草稿 |
| 版本号 | v1.0 |
| 目标平台 | Android / iOS（React Native） |
| 后端环境 | Ubuntu Server + 局域网 / IPv6 |

---

## 1. 项目概述

### 1.1 项目背景

用户希望在家庭局域网或 IPv6 网络内，通过手机随时流式播放存储在私有服务器上的音乐，同时保持对数据的完全掌控。本项目构建一套轻量、稳定、功能完善的私有流媒体音乐播放系统，替代依赖公网的商业流媒体服务。

### 1.2 核心目标

- 部署在私有 Ubuntu 服务器，支持局域网及 IPv6 访问，无需公网 IPv4
- 多用户注册登录，每位用户拥有独立的收藏与播放历史
- 音乐完全来自服务器本地文件系统，自动扫描、分类、入库
- 提供流畅的手机端（Android / iOS）播放体验，支持歌词展示
- 功能轻量聚焦，优先保证核心播放体验，后续可持续迭代

### 1.3 目标用户

- 家庭或小团体内的音乐爱好者，希望在私有环境下管理和收听个人音乐库
- 对隐私较为重视，不希望将音乐数据上传至第三方平台
- 具备基础服务器运维能力，可自行部署并维护后端服务

---

## 2. 技术架构

### 2.1 整体架构

系统采用前后端分离架构，后端提供 RESTful API 及音频流服务，前端为 React Native 跨平台移动应用。

| 层级 | 推荐技术栈 | 说明 |
|------|-----------|------|
| 移动端 | React Native（TypeScript） | Android & iOS 双端，单一代码库 |
| 后端 API | Node.js + Express / Fastify（TypeScript） | 全栈统一语言，生态丰富 |
| 数据库 | SQLite（via better-sqlite3） | 单机轻量部署，无需额外 DB 服务 |
| 音频流 | HTTP Range 请求 + chunked 传输 | 支持进度拖动与断点续流 |
| 认证 | JWT（Access + Refresh Token） | 无状态鉴权，多设备登录 |
| 文件扫描 | 后端定时任务 / 手动触发（music-metadata） | 递归扫描指定目录，提取元数据 |
| 网络接入 | 局域网直连 + IPv6 | 无需公网 IPv4，支持 mDNS 自动发现 |

### 2.2 网络访问方案

**局域网访问**
- 手机与服务器处于同一 Wi-Fi 时，通过局域网 IP（如 192.168.x.x）直连
- 推荐配置服务器静态 IP，避免 DHCP 导致地址变化
- 可选：启用 mDNS（Avahi），让 App 自动发现服务器（如 `streamsound.local`）

**IPv6 访问**
- 服务器配置全球单播 IPv6 地址，允许手机通过 IPv6 远程访问
- 需在路由器开放相应端口或配置 IPv6 防火墙规则

**连接配置**
- 首次使用时，App 提供「服务器地址配置」界面，用户手动输入 IP / 域名 + 端口
- 支持保存多个服务器配置，便于切换家庭与办公网络

---

## 3. 用户系统

### 3.1 账号注册与登录

- 支持用户名 + 密码注册，无需绑定手机号或邮箱
- 可选：管理员审核制，新注册账号需管理员审批后方可使用
- 密码使用 bcrypt 加密存储
- 登录成功后下发 JWT Access Token（有效期 1 小时）+ Refresh Token（有效期 30 天）
- Refresh Token 存储于设备安全存储（Keychain / Keystore）

### 3.2 用户角色

| 角色 | 权限说明 |
|------|---------|
| 管理员（Admin） | 管理音乐库扫描、审核新用户、查看用户使用情况、服务器配置 |
| 普通用户 | 播放音乐、搜索、收藏、查看个人播放历史，无法访问管理功能 |

---

## 4. 音乐库管理

### 4.1 音乐扫描机制

后端服务通过扫描指定根目录（默认 `/music`），递归遍历所有子目录，自动识别音乐文件并提取元数据。

**支持格式**：MP3、FLAC、AAC（M4A）、OGG、OPUS、WAV、AIFF、APE、WMA

**元数据提取优先级**：内嵌 ID3/Vorbis Tag > 文件名解析

**提取字段**：标题、艺术家、专辑、封面图（内嵌）、时长、比特率、采样率、歌词（LRC / 内嵌）

**扫描触发方式**：
- 服务启动时全量扫描
- 管理员手动触发
- 定时增量扫描（可配置，默认每小时一次）

### 4.2 文件夹分类逻辑

系统按文件系统目录结构生成「音乐文件夹」分类视图：

- **全部音乐**：根目录下所有音乐文件的聚合视图
- **子文件夹**：每个包含音乐文件的子目录自动生成同名文件夹视图
- 支持二级及以上子目录嵌套，每级均生成对应文件夹视图
- 不含音乐文件的空文件夹不显示

**示例**：`/music/` 下有 `日语歌曲/` 和 `中文歌曲/` 两个子目录，扫描结果生成三个文件夹视图：「全部音乐」「日语歌曲」「中文歌曲」。

### 4.3 元数据处理

- **封面图**：优先读取内嵌封面，无内嵌时查找同目录下的 `cover.jpg` / `folder.jpg` / `album.jpg`
- **歌词**：优先读取内嵌 LRC 歌词（ID3 USLT 标签 / Vorbis LYRICS 字段），其次查找同名 `.lrc` 文件
- **标题/艺术家为空时**：从文件名按「艺术家 - 标题.扩展名」格式解析，解析失败则以文件名作为标题
- **编码**：统一转换为 UTF-8 存储

---

## 5. 功能需求

### 5.1 功能优先级总览

| 功能模块 | 功能描述 | 优先级 | 备注 |
|---------|---------|--------|------|
| 用户注册 / 登录 | 用户名密码注册，JWT 认证，多设备登录 | P0 | 核心基础 |
| 音乐库浏览 | 按文件夹分类浏览，显示封面/标题/艺术家 | P0 | 核心基础 |
| 音频流播放 | HTTP Range 流式传输，格式自适应 | P0 | 核心基础 |
| 播放控制 | 播放/暂停/上下曲/进度条拖动 | P0 | 核心基础 |
| 播放队列 | 当前播放队列管理，支持增删调序 | P0 | 核心基础 |
| 随机/循环模式 | 随机播放 / 单曲循环 / 顺序播放 | P0 | 核心基础 |
| 歌词展示 | 内嵌/LRC 歌词逐行高亮滚动显示 | P0 | 核心基础 |
| 模糊搜索 | 按标题/艺术家/专辑模糊检索 | P1 | 重要功能 |
| 收藏功能 | 用户级收藏单曲，独立收藏列表 | P1 | 重要功能 |
| 最近播放 | 自动记录最近 50 首播放历史 | P1 | 重要功能 |
| 后台播放 | 锁屏/切后台持续播放，通知栏控制 | P1 | 体验提升 |
| 管理员控制台 | 扫描触发、用户管理（App 内） | P1 | 管理功能 |
| 离线缓存 | 本地缓存已播放曲目，断网可听 | P2 | 未来迭代 |
| 均衡器 | 10 段 EQ 调节 | P2 | 未来迭代 |
| 歌单功能 | 用户自建歌单，与收藏区分 | P2 | 未来迭代 |

### 5.2 播放功能

**播放器核心**
- 音频解码：利用平台原生解码器，支持 MP3/FLAC/AAC/OGG 等主流格式
- 流式加载：HTTP Range 请求，支持任意位置拖动跳转（无需等待完整下载）
- 进度条：显示当前时间 / 总时长，支持拖动，精度 1 秒
- 后台播放：切换 App 或锁屏后继续播放，系统通知栏显示上一曲/播放暂停/下一曲

**播放队列**
- 顺序播放队列，支持查看全部队列
- 从文件夹/搜索结果一键替换队列、或追加到队列末尾
- 长按拖拽调整顺序，左滑从队列移除
- 播放模式：顺序 → 随机 → 单曲循环，状态持久化

### 5.3 歌词功能

- 歌词区域位于播放页下方，占屏幕约 40% 高度
- 逐行显示，当前演唱行居中放大高亮，上下行逐渐缩小变淡
- 根据音频播放进度自动滚动至当前行
- 支持点击任意歌词行跳转至对应时间戳
- 无歌词时显示「暂无歌词」占位符
- 歌词文字大小可在设置中调整（小 / 中 / 大三档）

### 5.4 搜索功能

- 搜索范围：歌曲标题、艺术家名、专辑名，三者同时检索
- 支持模糊匹配
- 实时搜索：输入防抖 300ms 后自动触发，无需点击搜索按钮
- 结果分组：按「歌曲」「艺术家」「专辑」三组展示
- 搜索关键词在结果中高亮显示

### 5.5 收藏与历史

**收藏功能**
- 歌曲详情页及列表均有「收藏」按钮（心形图标）
- 收藏状态实时同步至服务器，多设备一致
- 收藏列表支持按收藏时间排序，支持取消收藏

**最近播放**
- 自动记录用户播放过的曲目，保留最近 50 条
- 每次播放同一首歌时更新时间戳（不重复记录，排序置顶）
- 显示相对播放时间（如「3 天前」），支持一键清空

---

## 6. 界面设计规范

### 6.1 设计原则

- **极简内容优先**：界面服务于音乐内容，减少视觉噪音，突出封面与歌词
- **无强品牌色**：采用近黑/近白/暗色调，避免鲜艳品牌色彩喧宾夺主
- **单手操作友好**：主要操作区域（播放控制、进度条）集中于屏幕下 1/3
- **流畅动效**：页面切换、歌词滚动采用平滑缓动，帧率目标 60fps

### 6.2 主要页面

| 页面名称 | 核心内容 |
|---------|---------|
| 登录 / 注册页 | 服务器地址配置、账号登录注册，简洁表单 |
| 音乐库首页 | 文件夹列表（网格/列表视图），最近播放、收藏入口 |
| 文件夹详情页 | 该分类下所有曲目列表，支持全部播放/随机播放 |
| 搜索页 | 搜索栏 + 实时结果（歌曲/艺术家/专辑分组） |
| 播放页（全屏） | 封面大图、歌词区域、进度条、播放控制、队列入口 |
| 播放队列页 | 当前播放队列列表，支持拖拽排序、模式切换 |
| 收藏列表页 | 已收藏歌曲，按时间排序 |
| 最近播放页 | 最近 50 首播放记录 |
| 设置页 | 服务器地址、账号管理、歌词字体大小、主题（亮/暗） |

### 6.3 迷你播放条

- 在非播放全屏页面底部常驻显示，显示封面缩略图、歌名、艺术家
- 提供播放/暂停按钮，点击展开全屏播放页
- 位于底部导航栏上方，不遮挡导航

---

## 7. 非功能需求

### 7.1 性能要求

- 音乐列表首次加载：< 1.5 秒（1000 首曲目以内）
- 播放启动延迟：点击播放到声音输出 < 1.5 秒（局域网环境）
- 搜索响应：输入防抖后结果返回 < 500ms
- 封面图加载：列表中封面异步加载，不阻塞列表滚动
- App 后台播放内存占用：< 100MB

### 7.2 可靠性

- 音频流中断自动重连（Wi-Fi 切换、短暂断网）
- 服务器异常时 App 给出友好错误提示，不崩溃
- 扫描任务失败不影响已有音乐库数据

### 7.3 安全性

- 所有 API 接口均需 JWT 鉴权，未登录用户无法访问音乐数据
- 建议在局域网内使用 HTTPS（自签名证书），防止明文传输
- 密码传输使用 HTTPS，服务端存储使用 bcrypt 哈希
- JWT 密钥通过环境变量配置，不硬编码

### 7.4 兼容性

- Android：最低支持 Android 8.0（API 26）
- iOS：最低支持 iOS 14.0
- 服务端：Ubuntu 20.04 LTS 及以上

---

## 8. 后端 API 概要设计

RESTful 风格，JSON 数据格式。

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/auth/register` | 无 | 用户注册 |
| POST | `/auth/login` | 无 | 用户登录，返回 Token |
| POST | `/auth/refresh` | Refresh Token | 刷新 Access Token |
| GET | `/library/folders` | JWT | 获取所有文件夹列表 |
| GET | `/library/folders/:id/tracks` | JWT | 获取文件夹内曲目 |
| GET | `/library/tracks/:id` | JWT | 获取单曲详情（含歌词） |
| GET | `/stream/:id` | JWT | 音频流（支持 Range） |
| GET | `/covers/:id` | JWT | 封面图 |
| GET | `/search?q=` | JWT | 全局模糊搜索 |
| GET | `/favorites` | JWT | 获取收藏列表 |
| POST | `/favorites/:id` | JWT | 收藏曲目 |
| DELETE | `/favorites/:id` | JWT | 取消收藏 |
| GET | `/history` | JWT | 最近播放记录 |
| POST | `/history/:id` | JWT | 上报播放记录 |
| POST | `/admin/scan` | JWT + Admin | 触发音乐库扫描 |
| GET | `/admin/users` | JWT + Admin | 获取用户列表 |

---

## 9. 开发计划（建议）

| 阶段 | 时间估算 | 主要工作 | 交付物 |
|------|---------|---------|--------|
| Phase 0 | 1 周 | 项目初始化：技术选型确认、开发环境搭建、数据库 Schema 设计 | 项目框架 |
| Phase 1 | 2 周 | 后端基础：用户认证、音乐扫描入库、文件夹 API、音频流 API | 可运行后端 |
| Phase 2 | 2 周 | App 基础：登录/注册页、音乐库浏览页、基础播放器（无歌词） | 可播放 Demo |
| Phase 3 | 1.5 周 | 播放进阶：歌词展示、播放队列、随机/循环模式、后台播放 | 完整播放体验 |
| Phase 4 | 1 周 | 搜索 + 收藏 + 历史：三大功能联调、本地缓存优化 | 功能完整版 |
| Phase 5 | 1 周 | 管理功能：管理员控制台（扫描触发、用户管理） | 管理员功能 |
| Phase 6 | 1 周 | 测试 + 优化：真机测试、性能优化、错误处理完善 | 正式版 v1.0 |

---

## 10. 风险与应对

| 风险描述 | 影响等级 | 应对措施 |
|---------|---------|---------|
| IPv6 网络环境不稳定 | 中 | 优先保证局域网功能，IPv6 作为扩展 |
| 音频格式兼容性（如 APE/WMA） | 低 | 优先支持 MP3/FLAC/AAC，其余格式逐步添加 |
| 歌词时间轴不准确 | 低 | 提供手动调整偏移量（+/- N 秒）设置项 |
| React Native 音频库局限性 | 中 | 采用 react-native-track-player 成熟方案，支持后台播放与通知栏控制 |
| 大型音乐库（万首）扫描慢 | 低 | 增量扫描 + 后台异步处理，不阻塞 API |

---

## 11. 待确认事项

开发启动前需确认以下决策点：

1. **新用户注册策略**：是否采用「管理员审核制」？封闭使用场景（仅家人/朋友）建议开启。
2. **iOS 支持**：是否需要支持 iOS 端？如为 Android 优先，iOS 可作为后续版本。
3. **多扫描目录**：音乐库根目录是否为单一路径，还是需要支持配置多个扫描根目录？
4. **歌词字体**：是否需要特别优化日语假名/中文歌词的字体渲染？
5. **收藏与歌单**：v1.0 是否区分收藏与歌单，还是统一使用「收藏」即可？

---

*Harmonia · PRD v1.0 · 内部文档*

# StreamSound · 版本号规则

---

## 1. 版本号格式

```
MAJOR.MINOR.PATCH[-TAG]
```

| 字段 | 说明 | 示例 |
|------|------|------|
| MAJOR | 重大版本，架构重构或不向后兼容的破坏性变更 | 1.x.x → 2.0.0 |
| MINOR | 新功能发布，向后兼容 | 1.0.x → 1.1.0 |
| PATCH | Bug 修复、性能优化、小改动，不新增功能 | 1.1.0 → 1.1.1 |
| TAG | 可选，标识非正式发布阶段 | `-alpha.1`、`-beta.2`、`-rc.1` |

---

## 2. 递增规则

- 升 **MAJOR**：MINOR 和 PATCH 归零（如 `1.4.3 → 2.0.0`）
- 升 **MINOR**：PATCH 归零，MAJOR 不变（如 `1.4.3 → 1.5.0`）
- 升 **PATCH**：仅 PATCH 递增（如 `1.4.3 → 1.4.4`）

---

## 3. 发布阶段标识（TAG）

| TAG | 含义 | 适用场景 |
|-----|------|---------|
| `alpha.N` | 内部测试，功能不完整，可能频繁破坏性变更 | 开发早期，仅内部使用 |
| `beta.N` | 功能基本完整，对外测试，可能存在已知问题 | 小范围用户测试 |
| `rc.N` | Release Candidate，候选正式版，原则上不再新增功能 | 上线前最终验证 |
| 无 TAG | 正式稳定版本 | 面向所有用户发布 |

`N` 从 1 开始递增，如 `alpha.1 → alpha.2 → beta.1`。

---

## 4. 前后端版本管理

StreamSound 前后端作为独立仓库分别维护版本号，通过 API 版本保证兼容性。

### 4.1 API 兼容性规则

- 后端 MINOR 升级时，须保持对上一个 MINOR 版本客户端的向后兼容
- 后端 MAJOR 升级时，需在 API 路径中体现版本号（如 `/v2/...`），旧版 API 保留至少一个 MINOR 周期后下线
- App 启动时检测服务器 API 版本，不兼容时提示用户升级

### 4.2 兼容性矩阵示例

| App 版本 | 后端最低要求 | 说明 |
|---------|------------|------|
| 1.x.x | 后端 1.0.0+ | 同 MAJOR 内保持兼容 |
| 2.0.0 | 后端 2.0.0+ | MAJOR 升级，需同步更新后端 |

---

## 5. 各阶段版本示例

对应 PRD 开发计划：

| 开发阶段 | 版本号 | 说明 |
|---------|--------|------|
| Phase 0–1（后端框架） | `0.1.0-alpha.1` | 内部初始版本 |
| Phase 2（基础播放可用） | `0.2.0-alpha.1` | 首个可演示 Demo |
| Phase 3（完整播放体验） | `0.3.0-beta.1` | 进入 Beta 测试 |
| Phase 4（功能完整版） | `0.4.0-beta.1` | 功能基本齐全 |
| Phase 5（管理功能） | `0.5.0-beta.2` | 管理员功能加入 |
| Phase 6（测试完成） | `1.0.0-rc.1` | 候选正式版 |
| 正式上线 | `1.0.0` | 首个稳定版本 |

---

## 6. 变更日志规范

每次版本发布须更新 `CHANGELOG.md`，条目按以下类型分类：

| 类型 | 说明 |
|------|------|
| `Added` | 新增功能 |
| `Changed` | 现有功能的变更 |
| `Fixed` | Bug 修复 |
| `Removed` | 移除的功能 |
| `Security` | 安全相关修复 |

**示例**：

```markdown
## [1.1.0] - 2025-09-01

### Added
- 歌单功能：用户可创建、编辑、删除自定义歌单
- 搜索结果支持拼音首字母模糊匹配

### Fixed
- 修复 Android 13 锁屏通知栏封面图不显示的问题
- 修复快速切歌时偶发的进度条卡死问题

## [1.0.1] - 2025-08-10

### Fixed
- 修复 IPv6 地址含方括号时连接失败的问题
```

---

*StreamSound · 版本号规则 · 内部文档*
